import { createSoundRegistry } from "@rbxts/sound-manager";

export const Sounds = createSoundRegistry({
    Test: {
        id: "rbxassetid://17771398985",
        volume: 1,
        loop: false,
    }
});
